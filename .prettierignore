# Legacy single-file apps and build tooling — keep untouched (huge hand-written
# HTML/CSS that Prettier would churn without benefit).
public/
legacy/
tools/
dist/
build-app.mjs
smoke-test.mjs

# Vendored agent skills (installed via `npx skills`) — third-party content,
# not ours to reformat.
.agents/
.opencode/
skills-lock.json